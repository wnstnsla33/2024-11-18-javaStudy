/*
 * && 직렬 연산자(AND)  
 * 
 * || 병렬 연산자(OR)
 * 
 * int x= 10;
 * int y= 9;
 * 
 * x<y && ++y ==false
 *     앞에 연산이 틀렸을 경우 뒤는 연산하지 않는다
 * x>y || y==x
 * 	   앞이 맞을 경우 뒤는 연산하지 않는다
 * 
 */

import java.util.Scanner;

public class 연산자_이항연산자_논리연산자 {
	 
}
